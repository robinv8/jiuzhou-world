---
import PageShell from '@/layouts/PageShell.astro'
import LinanHome from '@/views/linan/LinanHome'
import { getPageSeo } from '@/lib/seo'

const seo = getPageSeo('/linan')
const lang = 'zh' as const
---

<PageShell seo={seo} lang={lang}>
    <LinanHome lang={lang} />
</PageShell>
